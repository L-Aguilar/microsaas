{
  "installCommand": "npm install --force",
  "buildCommand": "npx vite build",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}